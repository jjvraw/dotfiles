[user]
	email = venter.joshua@gmail.com
	name = Joshua James Venter
[core]
	editor = nvim
	excludesfile = ~/.gitignore_global
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[init]
	defaultBranch = main
